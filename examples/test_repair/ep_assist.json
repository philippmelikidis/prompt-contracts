{
  "$schema": "../../src/promptcontracts/spec/schema/pcsl-ep.schema.json",
  "pcsl": "0.1.0",
  "targets": [
    {
      "type": "ollama",
      "model": "mistral",
      "params": {
        "temperature": 0.3
      }
    }
  ],
  "fixtures": [
    {
      "id": "password_issue",
      "input": "My password doesn't work anymore!"
    }
  ],
  "execution": {
    "mode": "assist",
    "max_retries": 2,
    "auto_repair": {
      "lowercase_fields": ["$.category", "$.priority"],
      "strip_markdown_fences": true
    }
  }
}
